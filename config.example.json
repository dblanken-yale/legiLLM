{
  "model": "gpt-4o-mini",
  "temperature": 0.3,
  "max_tokens": 2000,
  "description": "Configuration for LegiScan State Bill Analysis Pipeline",
  "notes": [
    "This is a basic configuration for the AI analysis passes",
    "API keys should be set as environment variables (PORTKEY_API_KEY, LEGISCAN_API_KEY)",
    "See config_examples/ directory for more advanced configuration options"
  ],
  "filter_pass": {
    "batch_size": 50,
    "timeout": 180,
    "description": "First pass: quick filter on bill metadata"
  },
  "analysis_pass": {
    "timeout": 90,
    "description": "Second pass: deep analysis with full bill text from LegiScan API"
  },
  "legiscan": {
    "cache_enabled": true,
    "cache_directory": "data/cache/legiscan_cache",
    "description": "LegiScan API configuration and caching settings"
  }
}
