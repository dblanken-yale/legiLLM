name: Deploy LegiUI to GitHub Pages

on:
  push:
    branches:
      - main
    paths:
      - 'legiUI/**'
      - '.github/workflows/deploy-legiui.yml'
  # Manual trigger via GitHub Actions UI
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: legiUI/package-lock.json

      - name: Install dependencies
        working-directory: legiUI
        run: npm ci

      - name: Build application
        working-directory: legiUI
        run: npm run build
        env:
          NODE_ENV: production
        # Note: public/bills.json is committed to git and automatically
        # copied to dist/ by Vite during build. To update the data,
        # run 'npm run load-data' locally and commit the updated public/bills.json

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './legiUI/dist'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
